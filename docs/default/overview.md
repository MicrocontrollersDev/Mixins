# What are these?

These are the default injectors that come with base mixin. More injectors are possible with other libraries, such as MixinExtras (has its own section) or MixinSquared (not included in this guide).
